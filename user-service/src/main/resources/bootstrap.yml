spring:
  application:
    name: user-service
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        enabled: true
      config:
        enabled: true
        file-extension: yml
